{
  "name": "ranking-checker-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install --include=dev && cd ../client && npm install --include=dev && npm run build",
    "start": "cd server && node server.js"
  }
}
